//** type inference=> you will understand by yourself(typescript under by yourself about default data type)
let drink = "chai"
let cups = Math.random() > 0.5 ? 10 : 5;

//** type anotations => it means i will explain to you what to to
// eg.(need to define the data type)
let flavour:string = "masala"
flavour="chocolate"

let order:boolean = false;